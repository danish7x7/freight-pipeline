"""In-memory mock implementations of the interface contracts.

These satisfy the Protocols in ``freight.interfaces`` and are the default backends
until the real implementations land in later phases.
"""
